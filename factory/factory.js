/**
 * Factory Pattern: Simplifies the object creation
 * Abstracts the process of creating objects and the configuration of the object 
 * Creates different objects based on the needs
 * Can used to get all dependencies in one place
 */